const { test, expect } = require('@jest/globals')
const HiringStations = require('../src/HiringStations')

describe('', () => {
    test('', ()=> {
        expect()
    })
})


// describe('Should throw error if the bag doesn\'t contain a weight?', () => {
//     test('has weight?', () => {
//         expect(() => new Bag()).toThrowError('bag must have a weight')
//     })
// })